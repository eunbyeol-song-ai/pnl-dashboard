# 프로젝트 규칙

이 문서는 이 프로젝트에서 항상 지켜야 할 규칙을 담고 있습니다.

## 규칙

1. **파일 경로를 코드에 하드코딩하지 않는다.**
   - 사용자가 그때그때 업로드/지정한 파일을 사용한다.
   - 파일 경로는 인자, 설정값, 또는 실행 시점에 전달받아 사용한다.

2. **엑셀·CSV 파일은 앱 폴더 밖에 두고, `.gitignore`로 제외한다.**
   - 데이터 파일(`*.xlsx`, `*.xls`, `*.csv` 등)은 앱 소스 코드와 분리된 별도 폴더에 보관한다.
   - `.gitignore`에 해당 확장자/폴더를 등록하여 저장소에 포함되지 않도록 한다.

3. **API 키는 코드에 직접 넣지 않고 `.env` 파일에서 읽는다.**
   - 모든 비밀 키, 토큰은 `.env`에 저장하고 코드에서는 환경 변수로 읽는다.
   - `.env` 파일도 `.gitignore`에 포함시켜 저장소에 커밋되지 않도록 한다.

4. **답변은 항상 한국어로 한다.**
